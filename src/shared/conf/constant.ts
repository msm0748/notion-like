export const DEFAULT_TITLE = '제목없음'

/** 페이지 타이틀·본문 공통 가로 패딩(px). 좌우 정렬 맞춤용 */
export const CONTENT_PADDING_X = 54

/**
 * BlockNote 사이드 메뉴·테이블 핸들 등이 본문 패딩 밖으로 삐져 나가지 않도록
 * 제목·에디터에 동일하게 더하는 가로 여유(px). 정렬은 CONTENT_PADDING_X와 합산값으로 맞춤.
 */
export const CONTENT_OVERLAY_INSET_X = 16
/** 페이지 타이틀·본문 공통 최대 너비(px) */
export const CONTENT_MAX_WIDTH = 900
